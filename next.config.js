

module.exports = {
    reactStrictMode: true,
    images: {
        domains: ['media.rawg.io'],
    },
    webpack(config, options) {
        config.module.rules.push();

        return config;
    }
}